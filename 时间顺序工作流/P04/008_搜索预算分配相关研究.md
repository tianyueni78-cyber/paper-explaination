# 搜索预算分配相关研究

## 结论

“自适应计算预算”已有先例。L116直接研究局部搜索计算预算的自适应分配；更多调度论文采用固定代数、固定时间或固定回合终止。本文不能把预算分配概念本身声明为首创。

本文预算b必须定义为本次最多允许发生的真实完整解码次数，并在实际调用完整解码器时逐次扣减。缓存命中、无完整解码的筛选或非法动作不应虚构扣减。所有方法以累计真实完整解码数作为共同主预算，墙钟时间作为补充成本。

跨论文的代数、时间、函数评价次数和完整解码次数不等价，因此相关论文只作机制或论文内比较，不把其报告值与本文结果直接横向排名。
