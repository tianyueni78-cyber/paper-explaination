# 自适应邻域选择相关研究

## 结论

RL选择邻域或算子已有明确先例，不能把“Q-learning选N”作为新贡献。L001在N1—N6间学习选择；L005在N1—N5间选择；L083根据历史表现动态管理算子组合并暂时排除低效算子。

已有机制的共同骨架是“状态/历史信息—动作算子—执行反馈—价值更新”。本文的可检验增量是动作从单独N扩展为`(K,N,b)`，使选择对象、操作方式和实际投入在同一决策中耦合；是否可学、是否因动作稀疏而失败必须由消融和学习行为日志回答。

因此P05仍需重新定义状态、动作档位、奖励、探索策略和Q表生命周期，不能直接照搬L001。
