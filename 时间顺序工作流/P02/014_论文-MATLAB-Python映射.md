# 论文—MATLAB—Python映射

## 结论

论文定义静态机器–AGV调度问题和QNSGA-II方法；MATLAB静态活动调用路径补足编码、初始化、解码和学习控制的可执行细节；Python A0分别映射为`data`、`chromosome`、`initialization`、`decoder`、`objectives`、`genetic`、`multiobjective`、`qlearning`、`neighborhoods`和`qnsga2`模块。

出现文字与活动代码差异时，A0按已确认裁决执行活动代码行为，并在差异文件中中性记录。
