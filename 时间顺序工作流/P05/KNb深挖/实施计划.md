# KNb深挖 Implementation Plan

> For agentic workers: 使用superpowers:executing-plans在本会话逐项实施；不自动启动子代理。

**Goal:** 回答区域、匹配、预算三个机制的增量价值。
**Architecture:** 原A0及S0只读。新probe_knb.py复用已核验的输入/计费/候选工具；analyze_knb.py负责探索选择与新验证统计；单独审计回放。
**Tech Stack:** Python标准库。
**Spec:** KNb深挖实验协议.md、KNb深挖候选定义.md。

## Global Constraints

静态、原N1—N6和目标不变；A0只读；总试验上限155520；最终保留Mk07/Mk08不读取；每项单独审查。依用户既有直接推送要求在paper-explaination/main独立目录工作，不改原算法仓库。

## 任务与验证

- [x] 等待关联：先写手算断言，例如p=2,d=5,a=7,s=11得到T=3,M=4；后继集合去重，再实现wait_features(data,schedule)。
- [x] 采集：capture(data,seed)返回0/9/19代独立位置，加入与无观测A0终态相等测试。
- [x] 连续预算：先测零预算、N4/N6内部计费、档案只增、相同输入重跑一致，再实现walk(data,parent,schedule,policy,fixed,budget,seed)。
- [x] D1/D2：先测训练选择不读取验证标签，再实现区域条件训练与固定/打乱比较。
- [x] 运行冒烟及主实验：python -B scripts/probe_knb.py --output runs/main；分析python -B scripts/analyze_knb.py runs/main。后经代码复审只修正重跑feedback，最终报告使用runs/budget-corrected。
- [x] 独立审计：全候选真实解码比对目标及合法性；轨迹每批预算和最终HV核算，源哈希复核。
- [x] 按scientific-critical-thinking分别写三个结果，按idea-evaluator裁决；第6步条件未获支持，未启动联合Q。
- [x] 更新成果清单，运行10项测试与git diff --cached --check；提交与推送状态以Git记录及本次交付消息为准。

测试文件scripts/test_knb.py，入口python -B scripts/test_knb.py。实现接口及具体断言随测试先写入，可直接作为实施规范；原始数据与实现一并提交。文档勾选只表达工作完成，不表达创新成立。
