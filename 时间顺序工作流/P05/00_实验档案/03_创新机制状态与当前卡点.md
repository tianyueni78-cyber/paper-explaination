# 创新机制状态与当前卡点

## 当前正式判断

论文研究范围仍是冻结静态A0局部搜索阶段的资源分配与学习控制。KNb分别表示搜索区域K、邻域N和本次允许消耗的真实完整解码预算b。

当前正式裁决是：

> 第三轮分解Q相对固定K25有增量，但未超过固定K50；第四轮进一步确认其复杂度没有带来相称收益。用户已终止当前自适应KNb联合组合创新点。固定K50作为简单策略保留，K×b作为未验证线索归档。

## 已测试机制

| 模块 | 已测试版本 | 当前证据状态 | 可以得出的结论 |
|---|---|---|---|
| K | 等权跨资源C | 有限支持 | 胜随机和机器区域，未稳定胜AGV单资源A |
| K | 等待关联L | 有限支持 | 胜W，未稳定胜A |
| K | 严格过滤K* | 部分完成 | 离线捕获部分N3/N4正候选；在线效果未关闭 |
| N | 区域条件N表 | 不支持当前实现 | 强区域内退化为固定N3 |
| N | 四状态N* | 不支持当前实现 | 未超过固定N3，状态覆盖退化 |
| b | 全程累计反馈 | 不支持当前实现 | 低于固定和简单停止 |
| b | 近期三批信用b* | 不支持当前实现 | 低于固定浅预算4 |
| 联合 | K*＋四状态N*＋近期b* | 不支持当前实现 | 111低于000，不能冻结 |
| 生命周期 | 当前反馈控制器按代数检查 | 局部信号 | 第0代有不精确信号；第1代已经转负 |
| 条件动作景观 | 24动作、同父解、分重复Oracle | 谨慎支持 | 相对强固定均值为正，5/6实例正向，区间略跨0 |
| 状态可预测性 | 12维决策前特征＋留一实例3近邻 | 不支持当前实现 | 5/6实例负向，95%区间全负 |
| K/N/b分轴 | 分重复Oracle | N轴谨慎支持 | N轴均值为正、5/6实例正且区间不跨0；K和b轴不支持 |
| 结构状态 | 新增8个动作前汇总＋留一实例3近邻 | 不支持当前实现 | 平铺1/6实例正向，贪心分层0/6实例正向 |
| 第三轮N | 可行性掩码＋分解Q | 不支持当前实现 | 对固定N3仅2/6实例正向，单位解码收益更低 |
| 第三轮K | K25/K50分解Q | 不支持动态选择 | 固定K50显著胜K25和Q选择K |
| 第三轮b | 4→8→12分批释放 | 不支持当前实现 | 低于固定b4 |
| 第三轮联合 | 五项分解Q＋信用衰减＋置信门控 | 不支持当前实现 | 111胜旧000但显著输固定K50 |
| 第三轮交互 | 八组析因 | K×b有限支持 | HV交互4/6实例为正且区间为正；效率区间略跨0，只限开发局部轨迹 |
| 第四轮归因 | 复用K-v1与interaction-v2 | 完成描述性归因 | K50改善可实施率和单位收益；N增加越界；模块交互不能改称纯K50×b |

## 固定策略F到底是什么

`F_A25_N3_b4`表示AGV等待区域A、区域比例25%、邻域N3、每批预算4。它不是原论文、MATLAB或冻结A0天然规定的真理，也不是“所有固定策略”的总代表。

本轮在每个留一实例折内只用另外5个训练实例重新选择最佳固定动作，6折均再次选择该动作。因此，它已成为本轮有独立选择依据的强固定对照，而不再只是早期探索数据中的经验选择。

实验结果仍只能表述为“某个具体自适应实现没有超过`F_A25_N3_b4`”，不能推广成“任何自适应策略都不如固定策略”。

## 已经回答的问题

1. 同一父解的有限动作中存在比强固定动作更好的条件选择上限，但效应小且区间略跨0。
2. K和N赢家会变化，b绝大多数选择4；这种变化不是简单、平滑的早中晚规律。
3. 当前12维决策前汇总特征不能在未见实例上预测优势动作。
4. 随后的第三轮已实施规则与分解Q开发实验；Bandit、完整优化器和保留数据仍未开始。

## 当前真正卡点

动作景观已经回答“有限动作空间有没有条件上限”：有，但很小且不精确。当前卡点已收窄为映射为什么失败：

\[
\pi(x_t)\rightarrow(K_t,N_t,b_t)
\]

已经回答：

1. 可重复机会主要来自N；当前K比例和一次性b档位没有独立支持。
2. 联合精确动作Top-1重复一致率仅30%，不适合直接学习24分类标签。
3. 简单全局与集合汇总没有补足信息；下一状态应描述N1—N6动作可实施机会、区域边际覆盖和在线预算信用。

不再继续回答纯K25/K50×固定/增量b交互，也不再开发新的含交互联合Q。相关问题保留为归档中的未验证事项，不能形成可部署控制器或论文贡献。

## 下一步边界

第四轮已解释固定K50同时改善候选可实施率与单位解码收益，也确认当前Q存在零解码/回退高、状态访问集中和K50选择率极低的问题。原K×b信号仍只能称模块耦合，因为实际K50和增量b动作都极少。当前KNb组合创新路线已终止；不做受控K×b追加实验，不打开保留数据。新的创新方向必须另立项、另冻结协议。
